fix: Correct map control button sizes on mobile devices

- Add map-control-button class to map control buttons (recenter and heatmap toggle)
- Update CSS to exclude map control buttons from global minimum size rules
- Map control buttons now display at intended 34px × 34px instead of forced 44px × 44px
- Maintains proper touch targets for all other buttons in the application
- Fixes oversized appearance of map control buttons on mobile devices
