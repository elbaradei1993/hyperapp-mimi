fix: Single-line button text on mobile with clean truncation

- Implement white-space: nowrap to keep button text on single line
- Add text-overflow: ellipsis for clean truncation with "..." when needed
- Increase min-width to 140px to minimize unnecessary truncation
- Use 20px padding on mobile for better text spacing and visual balance
- Maintain touch targets (44px min-height) while optimizing text display
- Keep quick action buttons flexible with normal text wrapping

Buttons now display clean single-line text on mobile with minimal truncation and proper spacing.
