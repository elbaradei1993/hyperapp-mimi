[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_ENABLE_QWEN = "false"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
